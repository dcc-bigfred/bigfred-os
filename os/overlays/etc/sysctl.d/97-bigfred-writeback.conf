# Align kernel dirty-page flush with /data ext4 commit=15 (centiseconds).
# Batches writes to flash without stacking large durability windows.
vm.dirty_writeback_centisecs = 1500
vm.dirty_expire_centisecs = 1500
vm.dirty_background_ratio = 5
vm.dirty_ratio = 15
