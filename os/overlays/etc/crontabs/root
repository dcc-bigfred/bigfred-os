# Hub log rotation (§8.9) — hourly; /data/logs is tmpfs 64m
0  *  *   *   *     /usr/sbin/rotate-hub-logs

# Persist wall clock (no RTC); restored by early-boot.sh
*/30 * * * * date -u +%s > /data/etc/fake-hwclock 2>/dev/null
