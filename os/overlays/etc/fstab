# Virtual filesystems (must be present for BusyBox / microinit / mount)
proc            /proc     proc   defaults            0  0
sysfs           /sys      sysfs  defaults            0  0
devpts          /dev/pts  devpts defaults,gid=5,mode=620  0  0

# Hub microSD layout: boot (p1 FAT) + root (p2 ext4) + data (p3 ext4)
# /data is NOT in fstab — early-boot.sh mounts it (NVMe p1 if migrated, else mmcblk0p3).
/dev/mmcblk0p2  /         ext4   ro,noatime          0  1

tmpfs           /tmp      tmpfs  defaults,size=64m   0  0
tmpfs           /var/log  tmpfs  defaults,size=16m   0  0
tmpfs           /var/run  tmpfs  defaults,size=16m   0  0
tmpfs           /run      tmpfs  defaults,size=16m   0  0
