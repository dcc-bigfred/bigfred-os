# bigfred-os-ui — hub admin web panel (dotenv)
# Edit on the device under /data/etc/ (persists across image updates).

HTTP=0.0.0.0:8090
PAM_SERVICE=bigfred-os-ui
LOG_ROOTS=/data/logs,/var/log
SECURE_COOKIE=false
